# Testing

## Run from `xiyou_solo` directory

```powershell
python -m unittest -v
```

## Run from repository parent directory

If your terminal is at `E:\VScodeProjects\xiyouTRPG`, use discovery:

```powershell
python -m unittest discover -s .\xiyou_solo\tests -v
```
